{% extends 'base.html.twig' %}

{% block title %}SB ✅{% endblock %}

{% block body %}

<div class="example-wrapper">
    <div>
        <h1> Agencia 0{{agencias.id}} </h1>
        <h4>Endereço: {{agencias.rua}} - {{agencias.bairro}} n° {{agencias.numero}}. {{agencias.cidade}} - {{agencias.uf}}</h4>
        <h4>Gerente: {{agencias.gerente}}</h4>

        <a href="{{ path('app_register') }}" class=" bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400 text-white rounded-md border p-2 mt-4 mb-2">
            Criar Conta
        </a>
    </div>

</div>

{% endblock %}
