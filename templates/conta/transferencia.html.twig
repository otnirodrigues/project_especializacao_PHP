{% extends 'base.html.twig' %}

{% block title %}Tranferir{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_row(form.valor) }}
        {{ form_row(form.contaDestino, { 'label': 'Conta Destino' }) }}
        {{ form_row(form.contaRemetente, { 'label': 'Conta Envio' }) }}


        <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400 text-white rounded-md border p-2 mt-4 mb-2">
            Transferir
        </button>
    {{ form_end(form) }}

    <div> 
        <a href="{{ path('app_home_page') }}">
            <button class=" bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400 text-white rounded-md border p-2 mt-4 mb-2">Cancelar</button>
        </a>
    </div>

{% endblock %}